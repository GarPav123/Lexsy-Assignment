{
  "name": "legal-document-processor",
  "version": "1.0.0",
  "description": "A web app for processing legal document templates with conversational interface",
  "main": "server.js",
  "scripts": {
    "start": "bun run server.js",
    "dev": "bun --watch server.js",
    "build": "cd client && bun run build",
    "install-client": "cd client && bun install",
    "heroku-postbuild": "bun run install-client && bun run build",
    "dev-full": "concurrently \"bun run dev\" \"cd client && bun run dev\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1",
    "docxtemplater": "^3.67.1",
    "pizzip": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": ["legal", "document", "template", "processing", "chat"],
  "author": "Lexsy Assignment",
  "license": "MIT"
}